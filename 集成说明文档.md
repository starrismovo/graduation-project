# 星海启航 - 前端集成说明文档

## 📦 文件清单

本次设计提供了以下文件：

### 核心页面组件

1. **CandidateHome.vue** - 重新设计的候选人首页
   - 路径：`src/views/CandidateHome.vue`
   - 功能：星海启航主题首页，岗位星系展示

2. **JourneyView.vue** - 星际航程（故事评估）页面
   - 路径：`src/views/JourneyView.vue`
   - 功能：沉浸式故事场景，替代原有的 AssessmentView

3. **JourneyReportView.vue** - 航行日志（报告）页面
   - 路径：`src/views/JourneyReportView.vue`
   - 功能：展示探索结果、能力雷达图、发展建议

4. **AvatarCreatorView.vue** - 虚拟形象创建页面
   - 路径：`src/views/AvatarCreatorView.vue`
   - 功能：创建/编辑用户的虚拟探险家形象

### 配置文件

5. **router-config.ts** - 路由配置
   - 路径：`src/router/index.ts`
   - 功能：定义所有页面路由

### 文档

6. **星海启航-前端设计方案.md** - 完整设计方案文档

---

## 🚀 快速开始

### 第一步：替换/添加页面组件

#### 1.1 替换候选人首页

原文件位置：`src/components/CandidateHome.vue`

**操作：**
```bash
# 备份原文件
mv src/components/CandidateHome.vue src/components/CandidateHome.vue.backup

# 使用新文件
cp CandidateHome.vue src/components/
```

**或者保留原文件，修改 `IndexView.vue` 的引用：**
```vue
<!-- src/views/IndexView.vue -->
<script setup>
// 原来：import CandidateHome from '../components/CandidateHome.vue'
import CandidateHome from '../components/CandidateHomeNew.vue' // 新版本
</script>
```

#### 1.2 添加新页面

将以下文件复制到对应目录：

```bash
# 核心页面
cp JourneyView.vue src/views/
cp JourneyReportView.vue src/views/
cp AvatarCreatorView.vue src/views/

# 创建占位页面（如果需要）
mkdir -p src/views/
# SelfDiscoveryView.vue 和 ConstellationMapView.vue 需要后续开发
```

### 第二步：更新路由配置

#### 2.1 合并路由

打开 `src/router/index.ts`，添加以下路由：

```typescript
const routes = [
  // ... 保留原有路由

  // 虚拟形象创建页
  {
    path: '/avatar-creator',
    name: 'AvatarCreator',
    component: () => import('../views/AvatarCreatorView.vue'),
    meta: { requiresAuth: true }
  },

  // 星际航程（核心评估页面）
  {
    path: '/journey/:id',
    name: 'Journey',
    component: () => import('../views/JourneyView.vue'),
    meta: { requiresAuth: true }
  },

  // 航行日志（报告页）
  {
    path: '/journey-report/:jobId',
    name: 'JourneyReport',
    component: () => import('../views/JourneyReportView.vue'),
    meta: { requiresAuth: true }
  },

  // 我的星图（个人中心）- 占位
  {
    path: '/constellation-map',
    name: 'ConstellationMap',
    component: () => import('../views/ConstellationMapView.vue'),
    meta: { requiresAuth: true }
  },

  // 自我发现测试 - 占位
  {
    path: '/self-discovery',
    name: 'SelfDiscovery',
    component: () => import('../views/SelfDiscoveryView.vue'),
    meta: { requiresAuth: true }
  }
]
```

#### 2.2 创建占位页面（可选）

如果暂时不开发这些页面，可以创建简单的占位组件：

```vue
<!-- src/views/ConstellationMapView.vue -->
<template>
  <div class="placeholder-page">
    <h1>我的星图</h1>
    <p>功能开发中...</p>
    <el-button @click="$router.push('/home')">返回首页</el-button>
  </div>
</template>

<script setup lang="ts">
</script>

<style scoped>
.placeholder-page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: #0a0e27;
  color: white;
}
</style>
```

同样方式创建 `SelfDiscoveryView.vue`。

### 第三步：安装依赖

确保已安装必要的依赖：

```bash
# Chart.js（用于雷达图）
npm install chart.js

# 或使用 yarn
yarn add chart.js
```

### 第四步：更新 API 接口（可选）

如果后端支持新的故事数据接口，在 `src/utils/request.ts` 中添加：

```typescript
// 获取故事模板
export function getStoryTemplate(jobId: string) {
  return request({
    url: `/api/story/${jobId}`,
    method: 'get'
  })
}

// 保存探索进度
export function saveJourneyProgress(data: any) {
  return request({
    url: '/api/journey/progress',
    method: 'post',
    data
  })
}

// 生成报告
export function generateJourneyReport(journeyId: string) {
  return request({
    url: `/api/journey/${journeyId}/report`,
    method: 'get'
  })
}
```

---

## 🔄 从原评估页面迁移

### 保留原页面作为后备

如果想保留原有的 `AssessmentView.vue` 作为后备方案：

1. **保持原路由**：
```typescript
{
  path: '/assessment/:id',
  name: 'Assessment',
  component: () => import('../views/AssessmentView.vue'),
  meta: { requiresAuth: true }
}
```

2. **在新首页中提供选择**：

在 `CandidateHome.vue` 中可以添加一个切换选项：

```vue
<el-button @click="useLegacyMode = !useLegacyMode">
  {{ useLegacyMode ? '使用新体验' : '使用经典模式' }}
</el-button>

<!-- 在开始评估时判断 -->
<script setup>
const useLegacyMode = ref(false)

const goToAssessment = (jobId) => {
  if (useLegacyMode.value) {
    router.push(`/assessment/${jobId}`)
  } else {
    router.push(`/journey/${jobId}`)
  }
}
</script>
```

### 数据兼容性

新旧系统的数据结构可能不同，建议：

1. **创建适配器**：

```typescript
// src/utils/dataAdapter.ts

// 将旧评估数据转换为新格式
export function convertLegacyToJourney(legacyData: any) {
  return {
    abilities: legacyData.scores.map(score => ({
      name: score.trait,
      description: score.reasoning,
      score: score.value
    })),
    // ... 其他转换
  }
}

// 将新探索数据转换为旧格式（兼容后端）
export function convertJourneyToLegacy(journeyData: any) {
  return {
    scores: journeyData.abilities.map(ability => ({
      trait: ability.name,
      value: ability.score,
      reasoning: ability.description
    })),
    // ... 其他转换
  }
}
```

2. **在 API 调用时使用适配器**：

```typescript
// 保存探索结果时
const saveResult = async () => {
  const legacyFormat = convertJourneyToLegacy(journeyData)
  await saveJourneyProgress(legacyFormat)
}
```

---

## 🎨 自定义与扩展

### 修改主题色

所有页面使用 CSS 变量，可以统一修改主题色：

```css
/* 在主样式文件或各组件中 */
:root {
  --primary-purple: #a78bfa;
  --primary-pink: #ec4899;
  --primary-orange: #f59e0b;
  
  --background-dark: #0a0e27;
  --background-card: rgba(30, 41, 59, 0.6);
  
  --text-primary: #e0e6ed;
  --text-secondary: #cbd5e1;
  --text-muted: #94a3b8;
}
```

### 添加新的故事章节

在 `JourneyView.vue` 的 `getStoryTemplate` 函数中添加新章节：

```typescript
{
  id: 'ch6',
  title: '第六章：新的挑战',
  subtitle: '未知领域',
  scenes: [
    {
      type: 'narration',
      content: '新章节的内容...'
    },
    // ... 更多场景
  ]
}
```

### 自定义虚拟形象

在 `AvatarCreatorView.vue` 中添加更多选项：

```typescript
// 添加新风格
const styles = ref([
  // ... 原有风格
  { id: 'fantasy', name: '奇幻', icon: '🧙' },
  { id: 'cyberpunk', name: '赛博朋克', icon: '🤖' }
])

// 添加新形象
const avatars = ref([
  // ... 原有形象
  { id: 13, emoji: '🦊' },
  { id: 14, emoji: '🐱' }
])
```

---

## 📊 故事数据结构规范

### 故事模板格式

```typescript
interface StoryTemplate {
  jobId: string
  jobName: string
  chapters: Chapter[]
}

interface Chapter {
  id: string
  title: string
  subtitle: string
  scenes: Scene[]
}

interface Scene {
  type: 'narration' | 'dialogue' | 'choice' | 'game' | 'feedback' | 'chapter_end'
  
  // 旁白场景
  content?: string
  
  // 对话场景
  messages?: Array<{
    speaker: 'npc' | 'player'
    speakerName: string
    text: string
  }>
  
  // 选择场景
  question?: string
  options?: Array<{
    id: string
    icon?: string
    text: string
    hint?: string
    abilitiesRevealed: string[]
    feedback?: string
    feedbackScene?: Scene
  }>
  
  // 游戏场景
  gameType?: string
  gameTitle?: string
  gameDescription?: string
  
  // 反馈场景
  feedbackTitle?: string
  feedbackText?: string
  abilitiesRevealed?: string[]
  
  // 插图（可选）
  illustration?: string
}
```

### 示例 JSON 文件

可以将故事数据保存为 JSON 文件：

```json
{
  "jobId": "1",
  "jobName": "前端工程师",
  "chapters": [
    {
      "id": "ch1",
      "title": "第一章：初入团队",
      "subtitle": "新的开始",
      "scenes": [
        {
          "type": "narration",
          "content": "你登上了星际飞船..."
        },
        {
          "type": "choice",
          "question": "你会如何开始？",
          "options": [
            {
              "id": "opt1",
              "text": "主动打招呼",
              "abilitiesRevealed": ["沟通能力", "主动性"],
              "feedback": "很好的开始！"
            }
          ]
        }
      ]
    }
  ]
}
```

将文件放在 `public/stories/` 目录下，然后在组件中加载：

```typescript
const loadStoryData = async () => {
  const response = await fetch(`/stories/${jobId.value}.json`)
  const storyData = await response.json()
  // ... 使用数据
}
```

---

## 🧪 测试建议

### 功能测试清单

- [ ] 首页加载正常
- [ ] 岗位卡片点击跳转到探索页
- [ ] 虚拟形象创建流程完整
- [ ] 故事场景切换流畅
- [ ] 选择分支正确记录
- [ ] 能力收集动画显示
- [ ] 章节完成统计准确
- [ ] 报告页雷达图渲染
- [ ] 分享功能正常
- [ ] 移动端适配良好

### 用户体验测试

1. **新用户流程**：
   - 登录 → 首页 → 创建形象 → 选择岗位 → 开始探索 → 查看报告

2. **老用户流程**：
   - 登录 → 首页 → 直接探索 → 查看历史星图

3. **多次探索**：
   - 完成一个岗位 → 返回首页 → 探索另一个岗位 → 对比报告

---

## 🐛 常见问题

### Q1: 路由跳转后页面空白

**原因**：组件路径错误或组件内部错误

**解决**：
1. 检查路由配置中的 component 路径
2. 打开浏览器控制台查看错误信息
3. 确保所有依赖都已安装

### Q2: 雷达图不显示

**原因**：Chart.js 未正确安装或 canvas 元素未找到

**解决**：
```bash
npm install chart.js
```

并确保 `onMounted` 中有延迟：
```typescript
onMounted(() => {
  setTimeout(() => {
    drawRadarChart()
  }, 300)
})
```

### Q3: 星空动画卡顿

**原因**：星星数量过多或设备性能较低

**解决**：减少星星数量
```vue
<!-- 从 50 减少到 20 -->
<div class="star" v-for="i in 20" :key="i">
```

### Q4: 虚拟形象不显示

**原因**：localStorage 数据未保存或格式错误

**解决**：
```typescript
// 检查数据
const avatarData = localStorage.getItem('userAvatar')
console.log('Avatar data:', avatarData)

// 清除并重新创建
localStorage.removeItem('userAvatar')
router.push('/avatar-creator')
```

---

## 📈 后续开发建议

### 短期（1-2周）

1. **完善占位页面**
   - 实现 `SelfDiscoveryView.vue`（自我发现测试）
   - 实现 `ConstellationMapView.vue`（我的星图）

2. **添加更多岗位故事**
   - 产品经理
   - UI 设计师
   - 数据分析师

3. **优化动画效果**
   - 场景切换过渡
   - 能力收集动画
   - 加载状态优化

### 中期（3-4周）

1. **小游戏开发**
   - N-Back 记忆任务
   - 反应时测试
   - 逻辑推理游戏

2. **数据分析功能**
   - 多次探索对比
   - 能力成长曲线
   - 岗位匹配推荐算法

3. **社交分享**
   - 证书图片生成
   - 社交平台分享
   - 好友挑战功能

### 长期（1-2月）

1. **AI 智能体集成**
   - 动态故事生成
   - 个性化反馈
   - 实时对话评估

2. **企业端功能**
   - HR 查看候选人报告
   - 自定义岗位故事
   - 批量评估管理

3. **高级可视化**
   - 3D 能力星图
   - 动态数据看板
   - 探索历程时间线

---

## 📝 更新日志

### v1.0.0 (2024-02-10)

**新增**
- 星海启航主题候选人首页
- 沉浸式故事评估页面（JourneyView）
- 航行日志报告页面（JourneyReportView）
- 虚拟形象创建页面（AvatarCreatorView）
- 完整路由配置

**改进**
- 采用星际探索主题替代传统评估界面
- 游戏化设计提升用户参与度
- 能力发现替代分数评判

**已知问题**
- 部分占位页面需要实现
- 小游戏组件待开发
- 后端 API 需要对接

---

## 🤝 贡献指南

如需添加新功能或修复问题，请：

1. 创建新分支
2. 遵循现有代码风格
3. 添加必要的注释
4. 测试所有功能
5. 提交 Pull Request

---

## 📧 联系方式

如有问题或建议，请联系项目负责人。

---

**祝你的星际探索之旅顺利！** 🚀✨
