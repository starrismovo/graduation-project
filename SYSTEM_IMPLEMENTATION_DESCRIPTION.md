# 基于AI智能体的人岗匹配评估系统功能实现

## 第五章 系统关键功能模块实现

关键功能模块的设计围绕候选人与HR两类用户展开，形成完整的招聘评估闭环。其中，候选人端主要完成岗位浏览、AI面试参与、评估报告查看和个人信息维护等功能；HR端主要完成岗位管理、候选人管理和招聘数据分析等功能。与第四章侧重评估模型设计不同，本章重点从系统实现角度说明各功能模块的页面组织、业务流程和前后端交互方式，并结合系统运行特性展示主要功能实现效果。

### 5.1 用户登录与身份识别模块

用户登录与身份识别是系统业务流程的入口，主要负责用户注册、登录认证、身份区分和页面访问控制。系统将登录与注册功能整合在统一入口中，通过界面切换方式完成不同模式的转换，从而降低用户的使用成本。

**页面设计与功能组织**

登录页面采用企业级设计风格，采用双栏布局。左侧为品牌展示区，包括AI人岗评估系统的品牌标识、系统名称、核心价值主张（"让人才与岗位更智能地相遇"）和AI插图，强化系统的智能化属性。右侧为登录/注册表单区，支持两种交互模式的快速切换。

在注册过程中，系统支持候选人与HR两类身份选择，并对用户名、邮箱和密码等字段进行前端校验，以保证基础数据的合法性和规范性。用户输入的密码在传输前进行哈希加密，在后端验证时采用盐值机制防止字典攻击，增强安全性。

**认证流程与权限管理**

用户登录成功后，后端返回包含用户ID、用户名、邮箱、身份标识（isHR）等信息的JSON对象，同时生成JWT令牌用于后续请求认证。系统将用户身份信息与认证令牌保存至前端状态管理（Pinia userStore）中，并根据用户角色跳转至对应首页。候选人进入候选人端首页，HR则进入招聘管理相关页面。

系统在前端路由层和后端接口层均进行权限检查。前端通过路由守卫（beforeEach）检查是否存在有效的认证令牌和相应的角色权限，若权限不足则自动重定向至首页或登录页。后端接口在处理请求前验证令牌的有效性，并检查用户身份是否符合接口要求。通过该方式，系统能够在前端路由和后端接口访问中区分不同用户角色，保证候选人和HR访问权限的相对隔离。

**技术实现细节**

- 前端：Vue 3组件使用`v-if`条件渲染区分登录和注册模式，使用Element Plus的el-input、el-button等组件构建表单。
- 后端：FastAPI `/auth/login`和`/auth/register`接口处理身份验证，使用SQLAlchemy ORM与MySQL数据库交互，存储用户账户和密码信息。
- 状态管理：Pinia store在首页（HomePage）根据`userStore.isHR`标志选择渲染候选人端或HR端首页。

该模块不仅承担基础身份认证功能，也为后续岗位浏览、评估会话创建、报告查看和岗位管理等业务流程提供用户上下文支撑。

---

### 5.2 候选人岗位浏览模块

岗位浏览是候选人的核心业务入口，主要功能是帮助候选人快速发现和理解目标岗位，并支持灵活的筛选和排序方式。

**页面布局与搜索设计**

岗位浏览页分为上中下三层结构。顶部为英雄区（hero section），包含页面标题"岗位中心"、当前系统中岗位总数的实时显示、搜索栏和操作提示。搜索栏采用组合式设计，支持自由输入职位名称或公司名，用户可按Enter键或点击搜索按钮触发查询。

中层为筛选控制区，从左至右依次为：排序选项（推荐、薪资高→低、薪资低→高、最新发布）、城市快速选择（通过API动态加载热门城市列表）、薪资范围选择（5k以下、5k-10k、10k-20k、20k-30k、30k以上五档）、重置按钮。下层为岗位卡片网格区，采用响应式布局，每行显示岗位卡片数量随屏幕宽度自动调整，支持12条/页的分页展示。

**数据筛选与排序逻辑**

前端组件维护以下数据状态：`searchKeyword`（搜索关键词）、`selectedCity`（选中城市）、`selectedSalary`（选中薪资档位）、`sortBy`（排序方式）、`currentPage`（当前页码）、`pageSize`（每页条数）。用户修改任何筛选条件时，系统将当前页码重置为1，以避免出现空页面。

排序功能采用混合策略：
- 当`sortBy==='recommended'`时，按岗位的`match_score`字段降序排列，优先展示与候选人匹配度最高的岗位。
- 当`sortBy==='salary_high'`时，按`salary_max`字段降序排列。
- 当`sortBy==='salary_low'`时，按`salary_min`字段升序排列。
- 当`sortBy==='latest'`时，按发布时间降序排列。

初次加载和筛选变化时，前端调用`/jobs/search` API，传递分页和筛选参数。后端应用过滤逻辑后返回数据，前端获得岗位列表和总数后，若需要本地排序优化（例如推荐排序），则在客户端侧应用排序函数。

**岗位卡片与交互**

岗位卡片使用独立的JobCard组件展示，每张卡片包含：
- 岗位名称、公司名、城市、薪资范围
- 岗位类别标签（可选）
- 大五人格需求的可视化展示（若岗位配置了人格特质需求，则展示相关维度）
- 匹配分数条形图或匹配标签（仅在用户已完成初评后显示）

用户交互方式：
- 点击卡片进入岗位详情页，传递`jobId`参数。
- 点击"立即参与评估"按钮，系统将`jobId`作为查询参数跳转至AI面试间。
- 点击"加入面试中心"按钮，将岗位保存至浏览器本地存储（interviewHub），以便后续快速开始面试。

**前后端交互流程**

1. 前端页面挂载时，调用`/jobs/filters` API获取城市列表和其他筛选选项。
2. 用户修改筛选条件，前端发起`/jobs/search` GET请求，参数包括`page`、`page_size`、`keyword`（可选）、`city`（可选）、`salary_min/max`（可选）、`sort_by`。
3. 后端应用数据库查询和过滤逻辑，返回`{items: [...], total: number}`格式的响应。
4. 前端接收数据，若需本地排序优化则应用排序函数。
5. 分页变化时触发滚动至顶部操作，改善用户体验。

---

### 5.3 岗位详情与需求解读模块

岗位详情页是候选人了解特定岗位的关键页面，系统通过详细的信息展示和人格需求解读，帮助候选人快速评估自身是否匹配。

**页面内容组织**

岗位详情页分为两列布局：左列为岗位基本信息、工作描述、任职要求等内容，右列为大五人格特质需求的可视化展示和说明。

岗位基本信息卡片显示：公司名、职位名、城市、薪资范围、发布时间、工作性质、学历要求等。岗位描述和任职要求采用纯文本或富文本展示，格式遵循后端数据库存储的格式。

**大五人格特质需求展示**

系统在岗位详情页中通过结构化的方式展示大五人格特质需求。每个特质维度包含以下信息：

1. **维度标识与可视化**：使用特定的颜色和图标代表各维度
   - 开放性（Openness）：蓝紫色#6B7FFF，图标🌟
   - 尽责性（Conscientiousness）：粉红色#FF6B9D，图标✓
   - 外向性（Extraversion）：橙色#FFA500，图标💬
   - 宜人性（Agreeableness）：绿色#00D084，图标🤝
   - 神经质（Neuroticism）：红色#FF5252，图标⚡

2. **分数与需求等级**：显示0-10分的评分，根据分数划分为三个需求等级
   - 高需求（≥7分）：背景色浅红，显示"高需求"标签
   - 中需求（4-7分）：背景色浅橙，显示"中需求"标签
   - 低需求（<4分）：背景色浅绿，显示"低需求"标签

3. **专业解释与岗位关联**：每个维度包含一段专业描述，说明该特质在岗位中的具体应用场景
   - 开放性解释：强调在岗位中需要接受新想法、学习新技能的重要性
   - 尽责性解释：强调严谨态度、细节处理、流程遵循的必要性
   - 外向性解释：强调团队沟通、客户交流的需求
   - 宜人性解释：强调团队合作、冲突解决、人际和谐的价值
   - 神经质解释：强调情绪稳定性、抗压能力的关键性

4. **进度条可视化**：使用进度条直观展示分数相对于10分满分的比例，进度条颜色与维度颜色保持一致。

**页面交互功能**

- **加入面试中心**：点击该按钮后，系统调用`addHubJob`函数，将岗位ID和基本信息保存至浏览器IndexedDB中的interviewHub存储库，并显示"已加入"状态。候选人可在"我的面试"页面查看已加入的岗位列表。
- **开始评估**：点击该按钮直接跳转至AI面试间，传递岗位ID作为查询参数，系统自动初始化该岗位的评估会话。
- **返回列表**：在页面头部提供返回按钮，使用`router.back()`方法返回岗位列表，保留用户之前的筛选和排序状态。

**数据加载与错误处理**

前端组件在挂载时从路由参数中提取`jobId`，调用`getJobDetail(jobId)` API获取岗位完整信息。若岗位ID无效或网络请求失败，系统显示错误提示"岗位编号无效"，并自动重定向至岗位列表页。后端`/jobs/{job_id}` GET接口返回包含`required_traits`字段的岗位对象，其中`required_traits`为一个JSON对象，键为特质维度名称，值为0-10之间的数值。

---

### 5.4 我的面试中心模块

我的面试中心是候选人管理面试过程的枢纽，支持查看待处理邀请、进行中的面试、已完成的面试等多个维度的内容。

**模块概览与Tab设计**

该模块采用Tab页面设计，包含以下页签：
- **邀请列表（Invitations）**：显示HR发送给候选人的邀请，分为"待处理邀请"和"已处理邀请"两个子区域。
- **我的面试（My Interviews）**：显示候选人的面试历史，按"进行中"、"已完成"、"其他状态"分类展示。
- **面试中心（Interview Hub）**：显示候选人主动加入的岗位列表，支持一键开启面试。

每个页签顶部显示对应内容的计数（例如"待处理邀请：3"），帮助用户快速了解操作待办项。

**邀请列表设计**

邀请列表项包含以下信息：
- 岗位名称、公司名、城市、薪资范围
- 邀请发起时间（显示相对时间，如"2小时前"，或绝对时间）
- 邀请状态标签：待处理（warning类型）、已接受（success类型）、已拒绝（info类型）、已过期（info类型）
- 操作按钮（仅对待处理邀请显示）：接受、拒绝

用户点击"接受"时，系统发起`/invitations/{invitation_id}/accept` POST请求，后端验证邀请有效性后，自动为候选人创建一个AssessmentSession，关联该岗位。系统提示"已接受邀请，可在面试列表中开始面试"。用户点击"拒绝"时，发起`/invitations/{invitation_id}/decline` POST请求，邀请状态更新为"已拒绝"。

**面试历史列表**

面试历史列表展示已创建的AssessmentSession记录，每条记录包含：
- 岗位信息（名称、公司、城市）
- 评估状态标签：进行中（warning类型）、已完成（success类型）、失败（danger类型）
- 评估开始时间、评估用时（若已完成）
- 参与的面试官数量
- 操作按钮：
  - 若状态为"进行中"，显示"继续面试"按钮，点击跳转至AI面试间。
  - 若状态为"已完成"，显示"查看报告"按钮，跳转至评估报告详情页。

**面试中心库（Interview Hub）**

面试中心库是一个本地浏览器存储功能，允许候选人在浏览岗位详情时"加入面试中心"，将岗位暂存本地，后续可快速开启面试而无需重新搜索。系统使用IndexedDB API实现该功能，在`/utils/interviewHub.ts`中提供`addHubJob`、`readHubJobs`、`removeHubJob`等工具函数。

面试中心库列表项显示已加入的岗位，支持：
- 快速开始面试：点击"开始面试"按钮跳转至AI面试间，传递`jobId`参数。
- 移除岗位：点击"移除"按钮从本地存储中删除该岗位。

**前后端交互流程**

1. 页面挂载时，调用`fetchHistory(candidateId)` API获取面试历史列表。
2. 同步调用`readHubJobs()`函数读取本地存储的面试中心岗位列表。
3. 调用`getCandidateInvitations(candidateId)` API获取邀请列表。
4. 页签切换时无需重新加载数据，仅更新展示的列表。
5. 用户操作（接受邀请、开始面试等）时，发起对应API请求，后端处理并返回结果，前端更新UI状态。

---

### 5.5 AI沉浸式评估对话模块

AI沉浸式评估对话是系统的核心评估功能，通过多轮Agent协同对话，在模拟真实面试的场景中评估候选人的大五人格特质、技能表现和潜力。

**评估流程与阶段设计**

系统将评估过程划分为四个阶段，每个阶段对应不同的评估目标和Agent角色组合：

1. **第一阶段：基础信息填充**
   - 目标：获取候选人的基本信息（姓名、学历、工作经验等）
   - Agent角色：待命状态，不直接参与评估
   - 前端交互：显示表单，候选人填写基本信息

2. **第二阶段：简历解析与初步评估**
   - 目标：通过简历分析初步了解候选人的背景和潜力
   - Agent角色：Resume Evaluator Agent开始评估
   - 前端交互：上传简历或输入简历文本，系统解析后生成初步评估结果

3. **第三阶段：全景综合评估**
   - 目标：通过结构化问卷和开放式问题，深度评估大五人格特质
   - Agent角色：Personality Evaluator Agent和Scenario Evaluator Agent根据岗位特点提问
   - 前端交互：多轮问答对话，候选人逐个回答系统生成的问题

4. **第四阶段：决策面试**
   - 目标：考察候选人在目标岗位场景中的决策能力和应变能力
   - Agent角色：Decision Agent提出岗位相关场景题，评估决策思路
   - 前端交互：通过对话形式呈现岗位特定的决策问题

**页面布局**

评估对话页采用三列布局：

- **左侧面板**（宽度约20%）：
  - 显示面试流程进度指示器，四个阶段以数字标记（1、2、3、4）
  - 每个阶段下方显示进度徽章（"已填充"、"进行中"等）
  - 背景为半透明白色卡片，叠加在主背景之上

- **中间对话区**（宽度约60%）：
  - 顶部为岗位信息栏，显示"当前面试岗位：[岗位名称]"，并附带岗位ID和面试状态标签
  - AI面试官信息区，显示AI头像、名称、职位
  - 实时情感分析面板（当进入第三阶段后激活），展示"候选人状态"标签（emotion字段）和"表达自信度"进度条
  - 消息流，展示AI提问和候选人回答的对话历史
  - 底部为输入框，支持文本输入和语音输入（若配置了语音识别服务）

- **右侧面板**（宽度约20%）：
  - 显示岗位基本信息：岗位名、公司、薪资、位置
  - 岗位相关性提示，突出显示该岗位的关键需求
  - 评估进度条，显示已回答问题数/总问题数

**消息流渲染与交互**

消息流按时间顺序显示所有对话内容。AI的消息显示AI头像和消息气泡，候选人的消息显示在右侧。每条AI消息下方包含以下交互元素：

- **回答输入框**：候选人在此输入回答文本
- **提交按钮**："回答并继续"按钮
- **跳过按钮**：用户可选择跳过当前问题（在允许的阶段中）
- **暂停/继续**：候选人可暂停评估，后续恢复时系统保持会话状态

系统实时保存候选人的回答，每次提交回答后，前端调用`/assessments/analyze-response` API，后端调用Personality Evaluator和Scenario Evaluator Agent分析回答，生成评分和反馈。前端接收分析结果后，调用`/assessments/next-question` API获取下一个问题，继续对话循环。

**实时情感分析**

系统在第三阶段及之后显示实时情感分析面板，包含两个指标：

1. **情感识别（Emotion Detection）**：系统通过分析候选人的文本（如使用NLP情感分类）或语音（如使用语音情感识别服务），识别候选人当前的情感状态（积极、中立、消极等），以标签形式展示。

2. **自信度评分（Confidence Score）**：系统根据候选人回答的内容完整度、用词确定性、逻辑清晰度等特征，计算一个0-100的自信度分数，用进度条可视化展示。

该功能帮助HR在查看报告时理解候选人的评估过程中的心理状态变化，为决策提供额外参考。

**进度保存与恢复**

当用户暂停评估时，系统调用`/assessments/save-session` API，后端保存当前AssessmentSession的所有状态信息：已回答的问题列表、各特质维度的临时评分、当前阶段号等。后续用户点击"继续面试"时，系统加载已保存的会话数据，恢复到之前的进度，允许继续回答问卷。

---

### 5.6 评估报告列表与详情模块

评估报告是评估过程的最终输出，以结构化报告的形式呈现候选人的大五人格评分、人岗匹配度和职业发展建议。

**报告列表设计**

报告列表展示候选人已完成的所有评估报告，支持以下功能：
- 按岗位、评估时间、匹配度等维度排序和筛选
- 快速导航至报告详情页
- 报告元信息展示：岗位名、公司、评估完成时间、综合匹配度

**报告详情页结构**

报告详情页是一个长篇幅的结构化报告，分为五个主要部分：

**1. 评估基本信息部分**

该部分以卡片式布局展示评估的元数据：
- 评估岗位：显示目标岗位名、公司、薪资范围
- 评估时间：显示评估开始和完成时间，以及总耗时
- 评估模式：显示此次评估采用的流程（例如"完整四阶段评估"）
- 参与评估的Agent数：显示有多少个Agent参与了此次评估（例如"3个评估Agent"）

**2. 匹配度概览部分**

该部分是报告的视觉中心，通过圆形进度指示器和维度卡片展示匹配度数据：

- **综合匹配度圆形指示器**：使用SVG绘制一个半径120的圆形进度条，中心显示百分比数值（0-100）和匹配等级标签（如"高度匹配"、"中等匹配"、"低匹配"）。圆形进度条的颜色根据匹配度区间动态变化：
  - 80-100%：绿色，标签为"高度匹配"
  - 60-80%：橙色，标签为"中等匹配"
  - 40-60%：黄色，标签为"基本匹配"
  - 0-40%：红色，标签为"低匹配"

- **匹配维度分析**：下方为三张平行的维度卡片，分别展示：
  1. **性格匹配度**（心理特质维度）：展示候选人的大五人格与岗位需求的匹配程度，如"候选人的开放性和尽责性与岗位需求高度吻合"。
  2. **技能匹配度**（能力维度）：显示候选人在岗位所需技能领域的能力水平评分。
  3. **潜力匹配度**（发展维度）：评估候选人在目标岗位的学习潜力和成长空间。

- **综合判断摘要**：在圆形指示器右侧提供一段自然语言摘要，说明综合匹配的关键因素和建议。

**3. 大五人格分析部分**

该部分通过数据可视化和文字分析深度解读候选人的性格特征：

- **人格雷达图**：使用radar图表库展示五个维度（openness、conscientiousness、extraversion、agreeableness、neuroticism）的评分，形成一个五边形的视觉化人格轮廓。每个维度的评分从0到10，在图表中表现为距离中心的距离，越远表示评分越高。图表的背景网格帮助用户快速理解各维度的相对位置。

- **优势表现（Strengths）卡片**：列举候选人在评估中表现出的主要优势，以标签形式展示（例如"高度自律"、"创新思维强"、"团队合作能力突出"）。系统根据大五评分和面试过程中的AI评价自动生成这些标签。

- **关注点（Gaps/Concerns）卡片**：列举候选人相比岗位要求的潜在短板或需要关注的方面（例如"情绪波动较大"、"细节关注度可提升"）。这些内容基于评分与岗位需求的差距计算得出。

- **人格解读摘要**：提供一段自然语言描述，综合分析候选人的性格特点、工作风格倾向、与岗位的匹配度，以及可能的发展方向。

**4. 职业发展建议部分**

该部分为候选人提供可执行的职业建议，分为两个方向：

- **推荐岗位方向**：基于候选人的性格特征，推荐其他可能高度匹配的岗位类型和行业方向，帮助候选人拓宽求职视野。例如"基于您的高开放性和外向性，适合考虑市场、运营、企业管理等岗位方向"。

- **谨慎投递方向**：根据评估结果，提醒候选人避免或谨慎投递的岗位特征，降低后续评估周期的浪费。例如"考虑到您的低尽责性评分，建议谨慎投递需要高度自律和流程规范的岗位（如质量保证、风险管理）"。

**5. 补充解读部分**（可选）

若系统在评估过程中发现特殊情况或有额外的专业建议，可在此部分展示补充内容，如：
- AI面试官的评价和观察
- 面试过程中的亮点记录
- HR工作笔记（若HR已添加）

**报告的前后端交互**

1. 前端从路由参数中提取`recordId`（AssessmentSession ID）。
2. 调用`/assessments/{recordId}/report` GET接口获取完整报告数据，包括：
   - `match_score`：综合匹配度百分数
   - `personality_traits`：大五人格评分对象
   - `trait_analysis`：性格分析文本
   - `strengths`：优势列表
   - `gaps`：关注点列表
   - `recommendations`：职业建议
3. 前端组件接收数据，使用Computed属性和过滤函数处理和转换数据，准备用于渲染。
4. 使用Vue组件如RadarChart呈现图表，使用Element Plus的标签组件呈现特征标签。
5. 提供导出报告为PDF、分享报告链接等扩展功能（若需求）。

---

### 5.7 HR岗位管理模块

HR岗位管理是招聘端的核心功能，支持岗位创建、编辑、发布和关闭，以及投递数据的监控。

**岗位管理仪表板概览**

岗位管理页采用仪表板设计，由统计卡片区和岗位列表区组成。

**统计卡片区**包含四个关键KPI：

1. **开放岗位**：显示当前HR发布且未关闭的岗位数
2. **总投递数**：显示所有已发布岗位的总投递/面试次数
3. **平均匹配度**：显示已完成评估的候选人的匹配度均值，帮助HR快速了解整体招聘质量
4. **待处理报告**：显示已完成评估但HR尚未查看的报告数，作为优先级提示

**岗位列表区**支持以下功能：

- **搜索**：按岗位名称或公司名搜索
- **排序**：支持三种排序方式
  - 最新发布：按岗位创建时间降序
  - 投递最多：按该岗位的投递数降序
  - 匹配度最高：按已完成评估的平均匹配度降序
- **岗位卡片**：每张卡片显示岗位名、公司、城市、薪资、投递数、平均匹配度、最后编辑时间、状态等

**岗位创建与编辑流程**

点击"创建岗位"按钮后，系统打开岗位编辑页（JobEditView），包含以下表单字段：

- **基本信息**：岗位名、所属公司、工作城市、薪资范围
- **岗位描述**：工作内容、岗位职责、任职要求（支持富文本编辑）
- **大五人格需求配置**：为五个维度各设置0-10的权重分数，表示该岗位对各维度的重视程度。系统提供预设模板（例如"管理岗"模板推荐高尽责性、高外向性），方便HR快速配置。
- **其他信息**：学历要求、工作经验、岗位类别标签等

保存岗位时，后端API (`/jobs` POST或`/jobs/{job_id}` PATCH) 将岗位数据存入数据库，并关联人格需求配置。

---

### 5.8 HR候选人管理工作台

HR候选人管理工作台采用优先级优化的设计，将HR最需要立即处理的操作集中在页面上方，下方为全量候选评估列表。

**页面构成与信息层级**

- **顶部焦点横幅**：显示当前选中岗位的基本信息和快速操作（"管理岗位"、"数据分析"）
- **HR操作优先区**：三个KPI卡片
  - 待响应邀请数：提示有多少候选人还未反馈邀请
  - 可查看报告数：提示有多少候选人已完成评估，可查看报告决策
  - 可邀请推荐数：基于推荐算法，提示有多少候选人符合邀请条件
- **"邀请首位推荐候选人"按钮**：一键操作，自动向推荐列表首位的候选人发送邀请，简化邀请流程

**候选评估表格**

表格以列表形式展示当前岗位的所有候选评估记录，包含以下列：

| 列名 | 内容 | 说明 |
|-----|------|------|
| 候选人 | 头像 + 名字 + 邮箱 | 展示候选人的身份信息 |
| 匹配度 | 进度条 + 百分比 | 可视化展示匹配度，支持色彩编码（绿>橙>红） |
| 状态 | 标签（进行中/已完成） | 评估过程状态 |
| 轮数 | 数字 | 评估进行的轮数 |
| 评估时间 | 日期时间 | 评估开始时间 |
| 操作 | 按钮 | 已完成显示"查看报告"，进行中显示"等待完成" |

**岗位快速切换**

页面上方的"选择岗位"下拉菜单允许HR快速切换岗位，选择新岗位后，表格数据自动刷新，显示该岗位的候选人列表。

---

### 5.9 招聘数据分析模块

数据分析模块为HR提供招聘过程的量化分析和洞察，支持多个维度的数据展示和筛选。

**分析维度**

- **招聘漏斗分析**：展示从投递 → 面试邀请 → 评估完成 → 决策通过的各阶段候选人数，以及各阶段的通过率。
- **岗位投递分布**：柱状图或饼图展示各岗位的投递数占比，帮助识别热门岗位。
- **候选人匹配度统计**：直方图展示所有评估候选人的匹配度分布（例如：50%的候选人匹配度在60-80%之间）。
- **趋势分析**：折线图展示一段时间内的投递数、邀请数、完成评估数等指标的时间趋势。

该模块可根据企业的具体需求进一步扩展，纳入更多业务指标和交叉分析。

---

### 5.10 用户个人信息维护模块

个人信息模块支持用户维护自身档案，包括候选人和HR两种用户。

**候选人个人信息页**

候选人个人信息页包含以下功能：

- **头像管理**：支持上传新头像，支持JPG、PNG格式，大小限制2MB以内
- **基础资料**：显示用户名（不可编辑）、昵称（用于系统内展示）、真实姓名、邮箱、电话、自我介绍（200字限制）
- **投递隐私设置**：候选人可选择投递和报告中的身份展示方式
  - 实名展示：使用真实姓名
  - 昵称展示：使用设置的昵称
  - 匿名编号：显示系统生成的编号，隐藏真实身份

该设置仅影响UI展示，不改变后端存储的实际数据。

**HR个人信息页**

HR的个人信息页类似候选人页，但字段说明和用途说明不同：

- **工作邮箱和联系电话**：用于候选人与HR的沟通联系
- **个人简介**：可填写招聘方向或公司背景信息
- **角色提示**：页面显示当前账号拥有岗位管理、候选人邀请、报告查看等权限

---

## 小结

系统的各功能模块围绕候选人和HR两条主线展开，形成完整的人岗匹配评估闭环。候选人端重点突出岗位发现、智能评估和报告查看的用户体验，HR端则聚焦于岗位和候选人的高效管理。在页面设计上，各模块遵循统一的企业级设计规范，采用卡片布局、清晰的信息层级和直观的数据可视化，确保用户能够快速获取关键信息和完成核心操作。在技术实现上，前后端的分工明确，前端负责UI交互和本地数据处理，后端负责复杂的业务逻辑、数据持久化和Agent协同，通过RESTful API实现高效的数据交互，确保系统的可扩展性和可维护性。
