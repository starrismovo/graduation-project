# HR 邀请码注册功能说明

## 功能概述

在注册页面中增加了用户类型选择功能，允许用户选择注册为 **普通用户** 或 **HR 管理员**。

- **普通用户**：应聘者和求职者，直接注册即可
- **HR 管理员**：需要企业邀请码验证才能注册

## 有效的 HR 邀请码

以下邀请码可用于 HR 管理员注册（任选其一）：

```
1. HR2026@STAR
2. ADMIN@RECRUIT
3. TALENT@MATCH
4. HIRING@PRO
5. RECRUIT@2026
```

> 💡 **提示**：邀请码区分大小写，请精确输入

## 注册流程

### 普通用户注册
1. 打开注册页面
2. 选择 "👤 普通用户"
3. 填写用户名、邮箱、密码
4. 点击"立即注册"完成

### HR 管理员注册
1. 打开注册页面
2. 选择 "👔 HR 管理员"
3. 输入框自动显示"HR 邀请码"字段
4. 填写用户名、邮箱、密码和有效的邀请码
5. 点击"立即注册"完成

## 技术实现

### 前端实现 (LoginView.vue)

**用户类型选择器**
- 两个单选按钮对应不同用户类型
- 样式美观，支持交互和视觉反馈

**邀请码验证**
- 仅在选择 HR 时显示邀请码输入框
- 前端客户端验证（对应固定邀请码列表）
- 无效邀请码时显示错误提示

**表单数据结构**
```typescript
registerForm = {
  username: string,        // 用户名
  email: string,           // 邮箱
  password: string,        // 密码
  userType: 'candidate' | 'hr',  // 用户类型
  inviteCode: string,      // HR邀请码（仅HR需要）
  is_hr: boolean           // 发送到后端的标志
}

// 有效邀请码列表
VALID_HR_INVITE_CODES = [
  'HR2026@STAR',
  'ADMIN@RECRUIT',
  'TALENT@MATCH',
  'HIRING@PRO',
  'RECRUIT@2026'
]
```

### 后端集成

后端现有的注册 API 无需修改：
```python
POST /auth/register
- username: str
- email: str
- password: str
- is_hr: bool  # 前端根据 userType 计算
```

前端在发送邀请码有效性验证后，计算 `is_hr = (userType === 'hr')`，发送给后端。

## 邀请码管理

### 如何修改邀请码

如需更改邀请码，在 `frontend/src/views/LoginView.vue` 中修改：

```typescript
// 第 ~320 行
const VALID_HR_INVITE_CODES = [
  'NEW_CODE_1',
  'NEW_CODE_2',
  // ... 添加更多邀请码
]
```

### 邀请码设计建议

所有邀请码遵循以下模式（便于识别）：
- 包含特定前缀（如 `HR-`, `ADMIN-`）
- 包含年份或版本号（如 `2026`）
- 使用大写字母和特殊字符
- 避免容易混淆的字符（如 `0` vs `O`, `1` vs `l`）

## 错误处理

前端验证错误提示：

| 错误 | 提示信息 | 解决方案 |
|------|---------|---------|
| 邀请码为空 | "请输入 HR 邀请码" | 填写邀请码 |
| 邀请码无效 | "❌ 邀请码无效，请确认输入正确" | 检查邀请码是否正确、区分大小写 |
| 其他验证错误 | 相应的错误提示 | 按提示修改 |

## 用户界面

### 注册类型选择

两个卡片式选项，支持以下交互：
- ✅ 点击选择用户类型
- ✅ 选中时高亮显示（蓝色边框和背景）
- ✅ 悬停时显示动画效果
- ✅ 清晰的图标和文字说明

### 邀请码输入框

仅在选择 HR 时显示，特点：
- ✅ 平滑动画（从上往下滑出）
- ✅ 带有"必填"标记
- ✅ 输入提示：含有邀请码图标
- ✅ 黄色提示框说明邀请码的用途

## 测试账户

**普通用户账户**
- 用户名: `testuser`
- 邮箱: `test@example.com`
- 密码: `123456`

**HR 测试账户** (使用邀请码注册)
- 用户名: `testhr`
- 邮箱: `testhr@company.com`
- 密码: `123456`
- 邀请码: `HR2026@STAR` (四选一)

## 常见问题

### Q: 邀请码能否通过后端验证？
**A**: 目前只在前端验证。如需加强安全性，可将邀请码管理迁移到后端数据库，但这超出了当前需求范围。

### Q: 用户注册后能否修改用户类型？
**A**: 不能。用户类型在注册时确定，后续通过管理员操作修改。

### Q: HR 用户能否像普通用户一样浏览岗位？
**A**: 是的。HR 和普通用户拥有相同的基础功能，只是在菜单和权限上有区别（如 HR 可以管理岗位）。

### Q: 邀请码是否有有效期？
**A**: 否。当前邀请码永久有效，除非手动在代码中更新。

## 安全建议

1. **定期更新邀请码** - 根据实际需求定期变更邀请码
2. **邀请码复杂度** - 使用足够复杂且难以猜测的邀请码
3. **后端强化** - 建议在生产环境中将邀请码管理迁移到后端数据库
4. **日志记录** - 记录使用邀请码注册的账户用于审计

## 后续优化方向

1. **邀请码数据库化** - 将邀请码存储在数据库，支持过期时间、使用次数限制
2. **邀请码生成工具** - 为管理员提供生成和管理邀请码的后台界面
3. **邀请码追踪** - 记录哪个邀请码被谁使用，用于审计
4. **动态邀请码** - 支持生成一次性或有时间限制的邀请码
